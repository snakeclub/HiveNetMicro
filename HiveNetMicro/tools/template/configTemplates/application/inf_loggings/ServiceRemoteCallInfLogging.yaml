  ServiceRemoteCallInfLogging:
    # 服务中远程调用的日志记录插件(日志标识不同)
    plugin:
      path: inf_logging_common.py
      class: CommonInfLoggingAdapter
      instantiation: True
      cache_id: ServiceRemoteCallInfLogging  # 指定cache_id生成新的实例
      init_kwargs:
        init_config:
          # 使用的日志标识
          logger: serviceLogger